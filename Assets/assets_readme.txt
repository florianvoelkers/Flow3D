To be able to import the model into the game engine I will need:

1. obj file of the model
2. .mtl file that linsk the model with the material; blender creates those when exporting the obj
3. texture or textures

You can find a working example in the directory WorkingExample. 